import Point from "../../../geometries/Point";
import Scene from "../../../Scene";
import Definition from "../../abstract/Definition";


export default class DefPoint extends Definition {

    constructor() {
        super();
    }

    /**
     * 
     * @param {Point} item 
     * @param {Scene} scene 
     */
    setup(item, scene) {
        super.setup(item, scene);

    }

    getPosition() {

    }

    applyMove(){
        
    }

}